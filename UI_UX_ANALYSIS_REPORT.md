# รายงานวิเคราะห์ UI/UX — เว็บไซต์ "ก่อนสมองพัง" (Before Brain Sick)

**วันที่วิเคราะห์:** 2026-08-15
**วิธีการ:** รัน dev server จริง (`npm run dev`), ตรวจ DOM/layout ด้วย JS ที่ viewport 375×812 (มือถือ) และ 1280×800 (เดสก์ท็อป), อ่านโค้ดทุกคอมโพเนนต์หลักใน `src/`
**Stack:** React 19 + Vite (ไม่ใช้ framework CSS ใดๆ, inline style + CSS variables ใน `src/index.css`)

หมายเหตุถึงผู้ที่จะนำไปแก้ไข (เช่น Gemini): ทุกข้อในรายงานนี้ตรวจสอบจริงบนเว็บที่รันอยู่ ไม่ใช่การเดาจากโค้ดอย่างเดียว — มี path + line number ระบุจุดแก้ไขให้ครบ

---

## สรุปภาพรวม

ดีไซน์เบื้องหลัง (design token, ระบบสี 3 ธีม light/dark/sepia, การจับคู่ฟอนต์ Prompt+Sarabun, callout box, badge system) ทำมาได้ดีและมีความสม่ำเสมอ แต่มี **บั๊ก layout จริงบนมือถือที่ยืนยันด้วยการวัดค่า DOM** และมีช่องโหว่ด้าน accessibility/keyboard navigation ที่ทำให้ผู้ใช้บางกลุ่มเข้าไม่ถึงเนื้อหาหลักของเว็บ (สารบัญ 20 บท) ได้เลย ซึ่งขัดกับเจตนาของเว็บที่ตั้งใจแจกฟรีเป็นวิทยาทาน

---

## 🔴 Critical — บั๊ก Layout จริงบนมือถือ (ยืนยันด้วยการวัด DOM)

### 1. โลโก้/ชื่อเว็บใน Navbar ล้นทะลุกรอบที่หน้าจอมือถือ
**ไฟล์:** `src/components/Navbar.jsx` บรรทัด 27-58

ที่ viewport 375px ปุ่มไอคอน 5 ปุ่ม (search, brain-tools, author, bookmark, theme) รวมกันกิน ~249px จากความกว้างทั้งหมด ทำให้เหลือพื้นที่ให้โลโก้ + ชื่อเว็บแค่ **~57px** ข้อความ "ก่อนสมองพัง" และ "นพ.วีระพันธ์ สุวรรณนามัย" จึงตกบรรทัดจนกล่องข้อความสูงถึง 144px ในขณะที่ตัว header (`.glass-nav`) ถูกกำหนดสูงคงที่ไว้ที่ 64px

ผลที่วัดได้จริง: `top: -40px, height: 144px` ภายใน container สูง 64px → **ข้อความล้นทะลุออกนอกกรอบ header ไปทับซ้อนกับเนื้อหาด้านล่าง** ทำให้ชื่อเว็บ/ชื่อผู้เขียนอ่านไม่ออกเป็นภาพบล็อกตัวหนังสือซ้อนกัน

**สาเหตุร่วม:** ไม่มี `@media` query อยู่ในโปรเจกต์เลยแม้แต่ตัวเดียว (grep `@media` ทั่ว `src/` ได้ 0 ไฟล์) ทำให้ทั้งเว็บพึ่งพา CSS Grid `auto-fit`/`auto-fill` และ `flex-wrap` เพียงอย่างเดียวในการจัดการ responsive ซึ่งไม่ครอบคลุมกรณี fixed-height flex row แบบ navbar

**คำแนะนำการแก้ไข:**
- เพิ่ม breakpoint (เช่น `@media (max-width: 640px)`) ให้ซ่อน subtitle "นพ.วีระพันธ์ สุวรรณนามัย" บนจอเล็ก เหลือแค่ "ก่อนสมองพัง"
- หรือย้ายปุ่มรอง (search, tools, author) เข้า hamburger/overflow menu บนมือถือ เหลือปุ่มหลักไว้แค่ 2 ปุ่ม (bookmark, theme)
- ให้ `.glass-nav` container ใช้ `min-height` แทน `height` คงที่ เพื่อไม่ให้เนื้อหาล้น

### 2. Header ซ้อนกัน 2 ชั้นตอนอยู่ในหน้าอ่านบทความ (กินพื้นที่จอ ~15%)
**ไฟล์:** `src/App.jsx` บรรทัด 95-118, `src/components/ReaderView.jsx` บรรทัด 153-232

`<Navbar>` ถูก render อยู่ตลอดเวลาไม่ว่าจะอยู่ view ไหนก็ตาม (App.jsx ไม่มีเงื่อนไขซ่อน Navbar เวลาเข้า reader) ในขณะที่ `ReaderView` ก็มี header ของตัวเองอีกชุด ทั้งคู่เป็น `position: sticky; top: 0`

**ผลที่วัดได้จริง:** ทั้งสอง header เกาะติดกันถาวรที่บนสุดของจอ (แม้ scroll ลงไปแล้วก็ยังอยู่) รวมความสูง **123px** จากจอสูง 812px (มือถือทั่วไป) = เกือบ **15% ของพื้นที่หน้าจอ** ถูกใช้ไปกับ chrome/navigation ตลอดเวลาที่อ่าน ซึ่งเป็นกิจกรรมที่ควรโฟกัสกับเนื้อหาที่สุด นอกจากนี้ปุ่ม bookmark ยังปรากฏซ้ำกันทั้งสอง header สร้างความสับสน

**คำแนะนำการแก้ไข:**
- ซ่อน `<Navbar>` หลักเมื่อ `activeView === 'reader'` เหลือแค่ header ของ ReaderView อย่างเดียว (ซึ่งมีปุ่ม back กลับสารบัญอยู่แล้ว)
- หรือรวม logic ให้ Navbar สลับเป็นโหมด "reader mode" แทนการซ้อนกัน 2 แถบ

### 3. ปุ่มไอคอนเล็กกว่ามาตรฐาน touch target
**ไฟล์:** `src/index.css` บรรทัด 284-292

`.btn-icon` กำหนดขนาด 40×40px ซึ่งต่ำกว่าเกณฑ์ขั้นต่ำ 44×44px (Apple HIG) / 48×48dp (Material Design) และมีปุ่มลักษณะนี้เรียงติดกัน 5 ปุ่มในแถวเดียวบน mobile navbar — เพิ่มโอกาสกดพลาด

**คำแนะนำ:** เพิ่มเป็น 44×44px อย่างน้อย หรือถ้าคง 40px ไว้ ให้ขยาย hit-area ด้วย padding/`::before` โปร่งใส

---

## 🟠 High — Accessibility / Keyboard Navigation

### 4. การ์ดที่คลิกได้ทั้งหมดใช้ `<div onClick>` แทน `<button>`/`<a>` (เข้าถึงไม่ได้ด้วยคีย์บอร์ด)
พบรูปแบบนี้ซ้ำใน **3 จุดหลักของเว็บ**:

| จุดที่พบ | ไฟล์ |
|---|---|
| การ์ดบทความในสารบัญ (รายการหลักของเว็บ 20 บท) | `src/components/ChapterList.jsx` บรรทัด 191-201 |
| รายการใน TOC drawer (สารบัญด่วนตอนอ่าน) | `src/components/ReaderView.jsx` บรรทัด 469-489 |
| ผลลัพธ์การค้นหาใน Search modal | `src/components/SearchModal.jsx` บรรทัด 110-124 |

ทั้งหมดนี้กด **Tab ไปไม่ถึง ไม่มี focus ring ไม่มี `role`/`tabIndex`** — ผู้ใช้ที่พึ่งคีย์บอร์ดหรือ screen reader **เข้าถึงเนื้อหาหลักของเว็บ (สารบัญ 20 บท) ไม่ได้เลย** ซึ่งขัดกับเจตนาของเว็บที่ทำเพื่อ "วิทยาทาน" ให้ทุกคนอ่านฟรีอย่างเท่าเทียม

**คำแนะนำ:** เปลี่ยนทั้ง 3 จุดจาก `<div onClick={...}>` เป็น `<button onClick={...}>` (ใส่ style ให้เหมือนเดิมได้ผ่าน CSS) หรืออย่างน้อยเพิ่ม `role="button" tabIndex={0}` พร้อม `onKeyDown` ดัก Enter/Space

### 5. Modal ปิดด้วยปุ่ม Escape ไม่ได้ ไม่มี focus trap
`SearchModal`, `AuthorModal`, `BrainToolsModal`, และ TOC drawer ใน `ReaderView` ปิดได้แค่คลิก backdrop หรือปุ่ม X เท่านั้น ไม่มี `useEffect` ดัก keydown `Escape` และไม่มีการ trap focus ไว้ในโมดัล (Tab สามารถหลุดออกไปโฟกัสเนื้อหาด้านหลังได้)

ปุ่มปิด "✕" ใน TOC drawer (`ReaderView.jsx` บรรทัด 462-464) เป็นตัวอักษรธรรมดา ไม่มี `aria-label="ปิด"` กำกับ

**คำแนะนำ:** เพิ่ม `useEffect` global keydown listener สำหรับ Escape ในทุกโมดัล และเพิ่ม `aria-label` ให้ปุ่มปิดทุกตัว

### 6. สถานะปุ่มบอกผ่าน `title` (tooltip) เพียงอย่างเดียว — ใช้ไม่ได้บนมือถือ
- ปุ่มขนาดตัวอักษร (`Type` icon) ใน ReaderView — `ReaderView.jsx` บรรทัด 208-219
- ปุ่มสลับธีม Sun/Coffee/Moon ใน Navbar — `Navbar.jsx` บรรทัด 114-121

ทั้งสองปุ่มไซเคิลสถานะ (เช่น small→medium→large→xl หรือ light→sepia→dark) โดยบอกสถานะปัจจุบันผ่าน `title` attribute (tooltip) เท่านั้น อุปกรณ์สัมผัส (มือถือ/แท็บเล็ต) ไม่มี hover จึงมองไม่เห็น tooltip เลย ผู้ใช้ต้องกดลองผิดลองถูกเพื่อจะรู้ว่ากดแล้วจะได้อะไร

**คำแนะนำ:** แสดงสถานะปัจจุบันเป็น label ข้อความเล็กๆ ข้างไอคอน หรือเปลี่ยนเป็น dropdown/segmented control ที่เห็นตัวเลือกทั้งหมดพร้อมกัน

---

## 🟡 Medium

### 7. ช่องค้นหาไม่มี `<label>` (ใช้ placeholder แทน)
`ChapterList.jsx` บรรทัด 78-93 และช่องค้นหาใน `SearchModal.jsx` บรรทัด 53-69 ใช้ placeholder เป็น label เพียงอย่างเดียว ซึ่งไม่ผ่านเกณฑ์ WCAG (`form-labels`) — screen reader บางตัวจะไม่ประกาศ placeholder เป็นชื่อฟิลด์

**คำแนะนำ:** เพิ่ม `aria-label="ค้นหาบทความ"` ให้ input โดยตรง (ไม่ต้องแสดงผลบนจอถ้าอยากคง placeholder ไว้)

### 8. Heading ย่อยในบทความถูกเดาจาก "ความยาวข้อความ" — เสี่ยง heading hierarchy ผิด
`ReaderView.jsx` บรรทัด 300-318 ใช้เงื่อนไข `if (p.length < 50 && !p.endsWith('.') && !p.includes('\n'))` เพื่อตัดสินว่าย่อหน้าไหนควร render เป็น `<h3>` แทน `<p>` — เป็นการเดาจาก heuristic ล้วนๆ ประโยคสั้นทั่วไปที่ไม่ได้ตั้งใจให้เป็นหัวข้อ (เช่น ประโยคเน้นย้ำสั้นๆ ที่ไม่ลงท้ายด้วยจุด) อาจถูกจัดเป็น heading โดยไม่ตั้งใจ กระทบโครงสร้าง heading ที่ screen reader ใช้นำทางเนื้อหา

**คำแนะนำ:** ย้าย logic นี้ไปทำตอนเตรียมข้อมูล (data pipeline ที่สร้าง `chapters.json`) แทน โดยมาร์กหัวข้อย่อยด้วย field ชัดเจน เช่น `{ type: "heading", text: "..." }` แทนการเดาจาก paragraph string เปล่าๆ

### 9. สีถูกเขียน hardcode ในหลาย component แทนการใช้ semantic token ที่มีอยู่แล้ว
เช่น `#10b981`, `#ef4444`, `#f59e0b` ใน `Hero.jsx` บรรทัด 134, 142 ทั้งที่ `src/index.css` มี token `--takeaway-border`, `--action-border` ที่ปรับตามธีมอยู่แล้ว (บรรทัด 35-39, 72-76, 109-113) การ hardcode สีตรงๆ ทำให้สีเหล่านี้ **ไม่เปลี่ยนตาม dark/sepia mode** เหมือนส่วนอื่นของเว็บ

**คำแนะนำ:** ไล่หาสี hex ที่เขียนตรงในทุกไฟล์ `.jsx` แล้วแทนที่ด้วย `var(--...)` ให้หมด

### 10. ใช้ Emoji เป็นไอคอนเชิงโครงสร้าง
พบ `⚠️` ใน `Footer.jsx` บรรทัด 130 (ข้อความ disclaimer) และ `🎉` ใน `ReaderView.jsx` บรรทัด 445 (ปุ่ม "อ่านครบทุกบทแล้ว") — emoji แสดงผลไม่เหมือนกันในแต่ละ OS/browser และปรับสีตามธีมไม่ได้ ควรใช้ SVG icon จาก `lucide-react` ที่เว็บใช้อยู่แล้ว (เช่น `AlertTriangle`, `PartyPopper`) แทนเพื่อความสม่ำเสมอ

---

## 💡 คำแนะนำเพิ่มเติมเพื่อให้เว็บดีขึ้น (นอกเหนือจากบั๊ก)

### Performance

1. **`chapters.json` หนัก 988KB ถูก bundle รวมเข้า JS bundle หลักทั้งก้อน** (`src/data/chapters.json`) แม้แต่ตอนเปิดหน้าแรก (home) ที่ยังไม่ได้เปิดอ่านบทไหนเลย ก็โหลดเนื้อหาเต็มทั้ง 20 บท (รวม `raw_text` ที่ใช้แค่ตอนค้นหา) ไปพร้อมกันหมด
   → แนะนำ: แยกไฟล์ต่อบท (`chapters/01.json`, `02.json`, ...) แล้ว `import()` แบบ dynamic เฉพาะบทที่เปิดอ่าน ส่วนหน้ารายการ/ค้นหาให้ใช้ metadata ไฟล์เล็กๆ (title, tags, quote, reading_time) แยกจาก `raw_text` เต็ม

2. **รูปภาพไม่มีการ lazy-load หรือกำหนด `width`/`height`/`aspect-ratio`** — `Hero.jsx`, `AuthorSection.jsx`, `AuthorModal.jsx` ใช้ `<img>` ธรรมดาไม่มี `loading="lazy"` (สำหรับรูปที่ไม่ใช่ hero) และไม่มีการจอง layout space ล่วงหน้า เสี่ยง Cumulative Layout Shift (CLS) ตอนรูปโหลด — รูปทั้งหมดยังเป็น `.jpg` (รวม ~820KB) ยังไม่ได้แปลงเป็น WebP/AVIF ที่จะลดขนาดได้มาก

3. **Google Fonts โหลดทั้ง 12 น้ำหนัก/สไตล์ของ 2 ฟอนต์** (`index.html` บรรทัด 14: `Prompt` 6 weight + `Sarabun` 6 weight) — ส่วนใหญ่เว็บไม่ได้ใช้ทุกน้ำหนัก แนะนำลดจำนวน weight ที่โหลดจริงเพื่อลด font payload

### UX / ฟีเจอร์ที่น่าเพิ่ม

4. **ไม่มีจุดจำตำแหน่งที่อ่านค้างไว้ในบทความ** — มี `readHistory` (อ่านแล้ว/ยังไม่อ่าน) แต่ไม่ได้บันทึก scroll position เพื่อ resume ตรงจุดที่อ่านค้าง ถ้าผู้ใช้ปิดแท็บกลางบทยาวๆ (บางบทอ่าน 20+ นาที) ต้องเลื่อนหาใหม่เอง

5. **ไม่มีปุ่มแชร์ไปยัง social โดยตรง** — มีแค่ "คัดลอกลิงก์" (`ReaderView.jsx` บรรทัด 125-131) ไม่มีปุ่มแชร์ไป Facebook/Line ตรงๆ ทั้งที่ผู้อ่านกลุ่มเป้าหมาย (คนไทย) นิยมแชร์ผ่าน Line เป็นหลัก

6. **ไม่มี breadcrumb/ตัวบอกความคืบหน้าระดับสารบัญ** ในหน้าอ่าน (เช่น "บทที่ 6 จาก 20") มีแค่ progress bar บาง 4px บนสุดที่บอกความคืบหน้า "ในบทนี้" เท่านั้น ผู้อ่านไม่รู้ว่าตัวเองอยู่ตรงไหนของทั้งเล่ม

7. **ปุ่ม "แบบเช็กสุขภาพสมอง" และเนื้อหา FAST Test อยู่ใน Modal เท่านั้น** ไม่มี URL/route แยกให้แชร์ลิงก์ตรงไปยังเครื่องมือนี้ได้ ถ้าอนาคตอยากให้คนแชร์เครื่องมือประเมินสุขภาพสมองต่อ ควรมี deep-link

8. **ไม่มี SEO structured data (JSON-LD)** สำหรับเนื้อหาแพทย์/บทความสุขภาพ (`Article`/`MedicalWebPage` schema) — จะช่วยให้ Google แสดงผลลัพธ์ค้นหาได้ดีขึ้นสำหรับเนื้อหาการแพทย์ที่มีคุณค่า E-E-A-T สูงแบบนี้

9. **ไม่มี `og:image`/`og:title`/Twitter Card meta tags** ใน `index.html` — เวลาแชร์ลิงก์ไป Facebook/Line จะไม่มี preview รูปสวยๆ ให้เห็น (สำคัญมากสำหรับเว็บที่ต้องการวิทยาทาน ยิ่งแชร์ง่ายยิ่งเข้าถึงคนได้มาก)

10. **Dark mode ยังไม่ auto-detect ระบบ** — ค่าเริ่มต้นดึงจาก `localStorage` เท่านั้น (`App.jsx` บรรทัด 15-17) ไม่ได้เช็ค `prefers-color-scheme` ของผู้ใช้ใหม่ที่ยังไม่เคยตั้งค่า

---

## สรุปลำดับความสำคัญที่ควรแก้ก่อน

1. **[Critical]** แก้ Navbar mobile overflow (#1) — เว็บใช้งานไม่ได้จริงบนมือถือตอนนี้
2. **[Critical]** ซ่อน/รวม header ซ้อนกันตอนอ่านบทความ (#2)
3. **[High]** เปลี่ยนการ์ด `div onClick` เป็น `button` ทั้ง 3 จุด (#4) — ผู้ใช้คีย์บอร์ด/screen reader เข้าไม่ถึงสารบัญเลย
4. **[High]** เพิ่ม Escape-to-close และ aria-label ให้ทุกโมดัล (#5)
5. **[Performance]** แยก `chapters.json` เป็นไฟล์ต่อบท + lazy-load (#1 ในหมวด Performance) — ลด initial bundle ~1MB
6. ที่เหลือ (#3, #6-10, และคำแนะนำเพิ่มเติม) ทยอยแก้ตามลำดับความสำคัญ/เวลาที่มี
