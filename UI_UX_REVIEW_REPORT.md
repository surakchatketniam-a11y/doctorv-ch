# UI/UX Review Report — หมอวี Health Library (before-brain-sick)

**วันที่ตรวจสอบ:** 2026-08-15
**เครื่องมือที่ใช้:** UI/UX Pro Max skill (accessibility, touch target, responsive, style, typography, navigation checklist)
**โปรเจกต์:** `D:\www\doctorv_channel\before-brain-sick` (React 19 + Vite 6)
**บริบท:** เว็บไซต์รวมเนื้อหาจากหนังสือ 2 เล่มของ นพ.วีระพันธ์ สุวรรณนามัย (หมอวี) — "ก่อนจะป่วย" (39 ตอน) + "ก่อนสมองพัง" (22 ตอน) = 61 บทความ

เอกสารนี้จัดทำเพื่อส่งต่อให้ Gemini (หรือ AI/นักพัฒนาอื่น) ดำเนินการแก้ไขต่อ กรุณาแก้ไขเรียงตามลำดับความสำคัญ (Critical → High → Low)

---

## 🔴 CRITICAL — ข้อมูลเสียหาย (Data Bug) ต้องแก้ก่อนเผยแพร่

### ปัญหา
ฟิลด์ `summary` ใน `src/data/all_chapters.json` ของบทความเล่ม "ก่อนสมองพัง" (bookId = `book2`) จำนวน **17 จาก 22 บท (77%)** ถูกใส่ค่าเป็น **ลิงก์ Facebook ดิบ** แทนที่จะเป็นคำโปรย/บทสรุปสั้นๆ ที่มีความหมาย

ตัวอย่างจริงจากไฟล์ (บรรทัด ~1994):
```json
{
  "id": "b2_02_บทที่_2_ระบบประสาท_เครือข่ายที่ซับซ้อนที่สุดในจักรวาล",
  "title": "บทที่ 2 — ระบบประสาท: เครือข่ายที่ซับซ้อนที่สุดในจักรวาล",
  "summary": "https://www.facebook.com/share/p/1bPeWnzLf4/?mibextid=wwXIfr...",
  ...
}
```

### ผลกระทบต่อผู้ใช้
ฟิลด์นี้ถูกแสดงเป็นคำโปรยใต้ชื่อบทความในหน้าสารบัญ (การ์ดบทความ) ที่:
- `src/components/ChapterList.jsx` บรรทัด 416-431 (การ์ดในหน้าแรก)

ผู้ใช้จะเห็นลิงก์ Facebook ที่ถูกตัดครึ่ง (`...wwXIfr...`) แทนคำโปรยที่ควรจะชวนอ่าน — กระทบความน่าเชื่อถือของเว็บทันทีตั้งแต่หน้าแรก และครอบคลุมเกือบทั้งเล่ม 2

### รายชื่อบทความที่ได้รับผลกระทบ (17 รายการ)
```
b2_02_บทที่_2_ระบบประสาท_เครือข่ายที่ซับซ้อนที่สุดในจักรวาล
b2_03_บทที่_3_สารสื่อประสาท_ผู้ส่งสารของสมอง
b2_04_บทที่_4_Neuroplasticity_สมองที่เปลี่ยนแปลงได้ตลอดชีวิต
b2_05_บทที่_5_โรคหลอดเลือดสมอง_(Stroke)_ฆาตกรเงียบในร่างกาย
b2_06_บทที่_6_โรคอัลไซเมอร์_เมื่อความทรงจำจางหาย
b2_08_บทที่_8_โรคพาร์กินสัน_มากกว่าแค่มือสั่น
b2_10_บทที่_10_โรคลมชัก_(Epilepsy)_ฟ้าผ่าในสมอง
b2_11_บทที่_11_ไมเกรน_ปวดหัวที่ซับซ้อนกว่าที่คิด
b2_12_บทที่_12_โรคซึมเศร้า_โรคของสมอง_ไม่ใช่ความอ่อนแอ
b2_13_บทที่_13_โรควิตกกังวล_เมื่อสมองตื่นตัวเกินไป
b2_14_บทที่_14_เนื้องอกสมอง_สัญญาณที่ไม่ควรมองข้าม
b2_15_บทที่_15_อาหารสำหรับสมอง
b2_16_บทที่_16_การออกกำลังกายและสมอง
b2_17_บทที่_17_การนอนหลับและสมอง
b2_18_บทที่_18_ความเครียดและสมอง
b2_19_บทที่_19_สังคมและสมอง_การเชื่อมต่อที่ช่วยชีวิต
b2_20_บทที่_20_สมองในยุค_AI_ภัยและโอกาสที่ต้องรู้
```

### วิธีตรวจสอบซ้ำ (reproduce)
```bash
cd before-brain-sick/src/data
node -e "
const data = require('./all_chapters.json');
const bad = data.filter(c => c.summary && /^https?:\/\//.test(c.summary.trim()));
console.log('affected:', bad.length, '/', data.length);
"
```

### แนวทางแก้ไขที่แนะนำ
1. เปิดไฟล์ `src/data/all_chapters.json` (และ/หรือ `book2_chapters.json` ถ้าเป็นไฟล์ต้นทาง — `all_chapters.json` ดูเหมือนถูก generate/รวมมาจาก `book1_chapters.json` + `book2_chapters.json`)
2. สำหรับแต่ละบทความในรายชื่อ 17 รายการด้านบน ให้แทนที่ค่า `summary` ที่เป็น URL ด้วยคำโปรยที่มีความหมาย โดยเลือกวิธีใดวิธีหนึ่ง:
   - **แนะนำ:** ดึงประโยคแรก (~120-160 ตัวอักษร) จาก `paragraphs[0]` ของบทความนั้นมาสรุปเป็น summary อัตโนมัติ แล้วให้คนช่วย proofread อีกครั้ง
   - หรือให้ผู้เขียน (หมอวี) เขียนคำโปรย 1-2 ประโยคใหม่ต่อบทให้เหมาะกับ SEO/social preview
3. ตรวจสอบว่าฟิลด์ `sourceUrl` (ซึ่งมี URL Facebook อยู่แล้วอย่างถูกต้อง) ไม่ได้ถูกปนกับฟิลด์ `summary` — ดูตัวอย่างที่ถูกต้องจากบทของเล่ม 1 เช่น `b1_ch_1`:
   ```json
   "sourceUrl": "https://www.facebook.com/share/p/1LZm3aAqCM/",
   "summary": "ทำความเข้าใจกลไก Homeostasis และ Allostasis ของร่างกายมนุษย์ที่พยายามปรับสมดุลและซ่อมแซมตัวเองอยู่ตลอด 24 ชั่วโมง"
   ```
   รูปแบบนี้คือมาตรฐานที่ถูกต้อง ให้ใช้เป็น template
4. หลังแก้ไขไฟล์ข้อมูลต้นทางแล้ว ให้รันสคริปต์ build/merge เดิม (ถ้ามีสคริปต์ generate `all_chapters.json` จาก source อื่น) เพื่อไม่ให้ค่าที่แก้ถูกเขียนทับกลับไปเป็น URL อีก — ถ้าไม่มีสคริปต์ ให้แก้ตรงที่ `all_chapters.json` ได้เลย
5. รัน `node -e "..."` script ข้อ (วิธีตรวจสอบซ้ำ) อีกครั้งเพื่อยืนยันว่า `affected: 0`

---

## 🟠 HIGH — สภาพแวดล้อมการพัฒนา

### โปรเจกต์ซ้ำที่อาจทำให้สับสน
พบโปรเจกต์สำเนาเก่าอยู่ที่ `D:\www\before-brain-sick` (นอกโฟลเดอร์ `doctorv_channel`) ซึ่งเป็นเวอร์ชันก่อนรวม 2 เล่ม และเคยรันอยู่ที่ port 3000 พร้อมกับตัวจริงที่ port 3001 — ถ้ามีคนแก้โค้ดที่โปรเจกต์ผิดตัว การเปลี่ยนแปลงจะไม่ขึ้นบนเว็บจริง

**แนะนำ:** ยืนยันกับทีมว่า `D:\www\doctorv_channel\before-brain-sick` คือ source of truth แล้วลบหรือ archive โฟลเดอร์ `D:\www\before-brain-sick` ที่ซ้ำซ้อนทิ้ง (ต้องมีคนยืนยันก่อนลบ เพราะเป็นการกระทำที่ทำลายข้อมูลถาวร)

---

## 🟡 LOW — หนี้เทคนิคเล็กน้อย (ไม่กระทบผู้ใช้ปัจจุบัน)

### ตัวแปรตั้งชื่อผิดความหมายใน ReaderView
ไฟล์: `src/components/ReaderView.jsx` บรรทัด 840
```jsx
const isRead = bookmarks.includes(c.id); // ควรเป็น readHistory ไม่ใช่ bookmarks
```
ตัวแปรชื่อ `isRead` ถูกคำนวณจาก prop `bookmarks` (บทความที่บุ๊กมาร์ก) ทั้งที่ชื่อบอกว่าควรเป็น "เคยอ่านแล้วหรือยัง" — ปัจจุบันค่านี้ไม่ได้ถูกเอาไปเรนเดอร์ที่ไหนใน TOC drawer จึงยังไม่กระทบ UI ที่เห็น แต่ควรแก้ให้ตรงความหมาย หรือลบทิ้งถ้าไม่ได้ใช้งานจริง (ปัจจุบัน `ReaderView` ไม่ได้รับ prop `readHistory` เลย ต้องส่งเพิ่มจาก `App.jsx` ถ้าต้องการใช้งานจริง)

---

## ✅ จุดที่ทำได้ดีอยู่แล้ว (ไม่ต้องแก้ — ใช้เป็นมาตรฐานอ้างอิงต่อ)

| หมวด | สถานะ | รายละเอียด |
|------|-------|-------------|
| Accessibility | ✅ | ทุกปุ่ม icon-only มี `aria-label`, มี `focus-visible` outline, contrast ratio ผ่าน 4.5:1 |
| Touch Target | ✅ | `.btn-icon` กำหนด min 44×44px ตามมาตรฐาน |
| Responsive | ✅ | breakpoint 640px/420px ทำงานถูกต้อง ทดสอบ 375px ไม่มี horizontal scroll |
| Theme System | ✅ | 3 โหมด (light/dark/sepia) ใช้ semantic CSS variables ทั้งหมด ไม่มี hardcoded hex ปนในคอมโพเนนต์ |
| Reader UX | ✅ | reading progress bar, font-size adjuster, TOC drawer, prev/next nav, confetti เมื่ออ่านจบเล่ม |
| Assets | ✅ | ไม่มีรูปภาพ 404 (ตรวจสอบผ่าน network requests แล้ว) |

---

## ลำดับความสำคัญในการแก้ไข (สำหรับ Gemini)

1. **[Critical]** แก้ฟิลด์ `summary` ที่เป็น URL ทั้ง 17 รายการใน `src/data/all_chapters.json` (และไฟล์ source ที่เกี่ยวข้องถ้ามี เช่น `book2_chapters.json`)
2. **[High]** แจ้งผู้ใช้/ทีมให้ยืนยันและจัดการโปรเจกต์ซ้ำที่ `D:\www\before-brain-sick`
3. **[Low]** แก้ไขหรือลบตัวแปร `isRead` ที่ผิดความหมายใน `ReaderView.jsx:840`

หลังแก้ไขเสร็จ ให้รันคำสั่งตรวจสอบซ้ำในหัวข้อ CRITICAL เพื่อยืนยันว่า `affected: 0` ก่อนปิดงาน
